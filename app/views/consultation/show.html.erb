<div>
	<iframe src="https://appear.in/<%= @consultation.room_name %>" width="800" height="640" frameborder="0"></iframe>
</div>

<div>
	<% if user_signed_in? %>
		<%= link_to "End Consultation", consultation_index_path %>
	<% elsif doctor_signed_in? %>
		<%= link_to "End Consultation", new_user_patient_review_path(params[:user_id]) %>
	<% end %>
</div>